export const fetchUser = (id: number) => {
  //   const response = 本当はAPI叩く
  const response = {
    age: 30,
    id: id,
    name: 'otanisan',
    sex: 'male',
    favorite: false,
  }

  return response
}
